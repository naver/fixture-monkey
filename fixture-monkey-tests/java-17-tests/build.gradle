java {
    toolchain { languageVersion = JavaLanguageVersion.of(17) }
}

dependencies {
    implementation project(path: ':fixture-monkey-api')
    testImplementation(project(":fixture-monkey-jackson"))

    implementation("net.datafaker:datafaker:2.0.1")
}
