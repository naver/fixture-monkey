---
title: "Concepts"
images: []
menu:
docs:
parent: "fixture-monkey-options"
identifier: "concepts"
weight: 54
---

Fixture Monkey provides numerous options that can be configured through the `FixtureMonkeyBuilder`. 
This page explains core option concepts to help you understand how they work together.

> **Navigating the Options Documentation**:
> * If you're just starting with Fixture Monkey, begin with [Essential Options for Beginners](../essential-options-for-beginners).
> * For complex test scenarios, see [Advanced Options for Experts](../advanced-options-for-experts).
> * For a complete list of remaining options, see [Other Options](../other-options).

## Core Components of Option Architecture

Understanding these core components will help you navigate the options more effectively:

### 1. Generators and Introspectors

The primary components that work together to create objects:

**Generator**:
- Creates arbitrary values for specific properties
- Examples: `StringArbitraryGenerator`, `IntegerArbitraryGenerator`

**Introspector**:
- Analyzes object structure to determine how to build instances
- Examples: `ConstructorPropertiesArbitraryIntrospector`, `FieldReflectionArbitraryIntrospector`

For a beginner, you can think of generators as the "what" (what values to create) and introspectors as the "how" (how to create the object).

### 2. Builder Pattern and Option Chaining

Fixture Monkey uses a builder pattern for configuration. Understanding option application order is important:

```java
FixtureMonkey monkey = FixtureMonkey.builder()
    .plugin(new KotlinPlugin())           // Applied first
    .nullableContainer(true)              // Applied second
    .defaultArbitraryContainerSize(3, 5)  // Applied third
    .build();
```

Options are applied in the order they're defined. Later options can override earlier ones if they target the same settings.

### 3. Option Scoping

Options can be applied at different scopes:

1. **Global Scope**:
   - Applied to all objects generated by the Fixture Monkey instance
   - Set in `FixtureMonkeyBuilder`

2. **Type Scope**:
   - Applied to specific types or interfaces
   - Example: `.pushAssignableTypePropertyGenerator(String.class, generator)`

3. **Expression Scope**:
   - Applied to specific properties identified by expressions
   - Example: `.register(String.class, fixture -> fixture.minSize(5))`

The more specific scope overrides the more general one.

### 4. Type Registration System

Fixture Monkey's type registration system determines how to generate values for specific types:

```java
FixtureMonkey monkey = FixtureMonkey.builder()
    .register(String.class, fixture -> fixture.minSize(5).maxSize(10))
    .register(Integer.class, fixture -> fixture.range(1, 100))
    .build();
```

This registers custom generators for specific types, which can then be used in all objects that contain these types.

### 5. Plugin System

Plugins provide pre-configured settings for common use cases:

```java
FixtureMonkey monkey = FixtureMonkey.builder()
    .plugin(new KotlinPlugin())
    .plugin(new JacksonPlugin())
    .build();
```

Plugins can:
- Register type-specific generators and introspectors
- Configure default behaviors
- Add support for third-party libraries

## How Options Interact

When Fixture Monkey generates an object:

1. It first determines which introspector to use for the object
2. The introspector analyzes the object structure
3. For each property, it selects the appropriate generator
4. Property generators create values according to configuration
5. The introspector assembles the final object

Options at more specific scopes take precedence over more general ones.

## Visual Explanation

Here's a simplified view of how these components interact:

```
┌─────────────────────────────────────────┐
│           FixtureMonkeyBuilder          │
└───────────────┬─────────────────────────┘
                │
                ▼
┌───────────────────────────────────┐    ┌───────────────────┐
│          Option Resolution         │◄───┤     Plugins      │
└───────────────┬───────────────────┘    └───────────────────┘
                │
                ▼
┌───────────────────────────────────┐
│       Type/Property Selection      │
└───────────────┬───────────────────┘
                │
                ▼
┌──────────────┐      ┌──────────────┐
│ Introspector │◄─────┤  Generators  │
└──────────┬───┘      └──────────────┘
           │
           ▼
┌───────────────────────────────────┐
│          Generated Object          │
└───────────────────────────────────┘
```

## Example: Option Interaction

Consider this example where multiple options interact:

```java
FixtureMonkey monkey = FixtureMonkey.builder()
    .plugin(new JavaTimePlugin())                              // Global plugin
    .defaultArbitraryContainerSize(1, 5)                       // Global container size
    .nullableContainer(true)                                   // Global container nullability
    .register(String.class, fixture -> fixture.minSize(3))     // Type-specific configuration
    .register(
        ExpressionMatcher.exactPath("order.items.price"), 
        fixture -> fixture.greaterThanOrEqualTo(BigDecimal.ZERO)
    )                                                          // Expression-specific configuration
    .build();
```

When generating an object:
1. The JavaTimePlugin configures date/time types
2. Containers are set to have 1-5 elements and can be null
3. All String values have minimum length 3
4. Order item prices are always non-negative

## Conclusion

Understanding these concepts will help you:
- Configure Fixture Monkey more effectively
- Troubleshoot issues when generators don't work as expected
- Create complex test data with precise control

## Next Steps

After understanding these concepts, explore:

→ [Essential Options for Beginners](../essential-options-for-beginners) - Start with the most commonly used options

→ [Advanced Options for Experts](../advanced-options-for-experts) - For complex testing scenarios

## Property
In the documentation, the term  `property` is consistently used instead of `field` when referring to the characteristics of a class's object.
While it has the same name as 'property' in Kotlin, the concept is different in Fixture Monkey.

Fixture Monkey's initial structure was primarily based on fields, which imposed limitations on configuration and control through methods and other mechanisms.
For example, when relying solely on fields, it's not possible to access annotations on setter methods.
To address this limitation, the `Property` interface was introduced, which extends support beyond just fields.

A `property` in Fixture Monkey, functions as a fundamental component within a class and can represent a `Field`, `Method`, or Kotlin `Property`.
It contains information about its `Type`, `Annotation`s on it, and its `name`.

Moreover, in Fixture Monkey, the characteristics of both `Objects` and `Containers` are also expressed through the concept of `property`.

### ObjectProperty
An `ObjectProperty` is a property that represents immutable object information. It includes:

- **property**: The property of the object itself.
- **propertyNameResolver**: Determines how the property name is resolved.
- **elementIndex**: If the object is an element of a Container, it indicates the index.

```java
public final class ObjectProperty {
    private final Property property;

    private final PropertyNameResolver propertyNameResolver;
	
    @Nullable
    private final Integer elementIndex;
}
```

### ContainerProperty
The property of a container type is represented by `ContainerProperty`, which describes immutable container information. It includes:

- **elementProperties**: A list of element properties.
- **containerInfo**: The`ArbitraryContainerInfo` that determines the container's size.

```java
public final class ContainerProperty {
    private final List<Property> elementProperties;

    private final ArbitraryContainerInfo containerInfo;
}
```

## Options
In Fixture Monkey, several options share common characteristics.
For example, let's take a look at the options related to modifying the `ObjectPropertyGenerator`

> `defaultObjectPropertyGenerator`, `pushObjectPropertyGenerator`, `pushAssignableTypeObjectPropertyGenerator`, `pushExactTypeObjectPropertyGenerator`

Options with the prefix default are applied as defaults to all properties generated by Fixture Monkey.
These defaults set a basic behavior that affects all property types uniformly.

However, if you need to apply specific options for a particular type, you can make use of the options that start with push.
There are three variations of these push options.

- `push~` : Accepts a MatcherOperator as a parameter.
- `pushAssignableType~`: This option applies the specified setting to every property type for which the given type (associated with the option) is assignable. This means that the option is applied not only to the exact given type but also to any type that can be assigned to the property type, including superclasses or superinterfaces.
- `pushExactType~`: This option limits the setting to properties with the exact same type. It does not impact properties with subtype or supertype relationships.

It's important to note that options set using the `push` variants take precedence over the `default` options. This means that when a `push` option is defined for a specific type, it will override any corresponding `default` option for that type.

```java
// Example of precedence in action:
FixtureMonkey fixtureMonkey = FixtureMonkey.builder()
    .defaultNullInjectGenerator(context -> 0.0)  // 1. By default, nothing is null
    .pushExactTypeNullInjectGenerator(String.class, context -> 1.0)  // 2. Strings will be null
    .build();

Product product = fixtureMonkey.giveMeBuilder(Product.class)
    .set("name", "Override")  // 3. This specific value overrides the null setting
    .sample();

// Result: product.name will be "Override" despite the String null setting
```

---

Understanding these concepts will help you use Fixture Monkey's options more effectively to create the exact test data you need. In the next sections, we'll explore specific options you can use to customize your test objects.

## Container Types vs. Object Types

Fixture Monkey treats container types (like `List`, `Set`, or `Map`) differently from regular object types.

**Why this matters for your tests:** Understanding this distinction helps you correctly set up tests that involve collections or maps.

For example, if you need to test a shopping cart with multiple products:

```java
// Creating a list of products
List<Product> productList = fixtureMonkey.giveMeBuilder(new TypeReference<List<Product>>() {})
    .size(3)  // Specify you want 3 products
    .set("[0].name", "First Product")  // Set property of the first element
    .sample();
```

## Option Prefixes and When to Use Them

Fixture Monkey options often use prefixes like `default`, `push`, `pushAssignableType`, and `pushExactType`. Each prefix has a specific purpose:

1. **default~**: Applies to all types globally
2. **push~**: Applies to specific types based on a matcher
3. **pushAssignableType~**: Applies to a type and its subtypes
4. **pushExactType~**: Applies only to the exact type specified

**Why this matters for your tests:** Understanding these prefixes helps you apply customizations at exactly the right scope:

```java
// Global default for all string properties
fixtureMonkey = FixtureMonkey.builder()
    .defaultNullInjectGenerator(context -> 0.0) // No properties will be null by default
    .build();

// Only String properties will use max length of 10
fixtureMonkey = FixtureMonkey.builder()
    .plugin(
        new JqwikPlugin()
            .javaTypeArbitraryGenerator(new JavaTypeArbitraryGenerator() {
                @Override
                public StringArbitrary strings() {
                    return Arbitraries.strings().ofMaxLength(10);
                }
            })
    )
    .build();

// Only Product price property will always be positive
fixtureMonkey = FixtureMonkey.builder()
    .register(Product.class, fixture -> 
        fixture.giveMeBuilder(Product.class)
            .set("price", Arbitraries.doubles().greaterThan(0))
    )
    .build();
```

## Option Precedence Simplified

When multiple options could apply to the same property, Fixture Monkey uses the following precedence (from highest to lowest):

1. Specific customizations in your builder (using `.set()`)
2. Type-specific options (`push` variants)
3. Global defaults (`default` options)

**Why this matters for your tests:** This allows you to set general rules with defaults, override them for specific types with push options, and then make individual adjustments with `.set()` - giving you multi-level control over your test data.

```java
// Example of precedence in action:
FixtureMonkey fixtureMonkey = FixtureMonkey.builder()
    .defaultNullInjectGenerator(context -> 0.0)  // 1. By default, nothing is null
    .pushExactTypeNullInjectGenerator(String.class, context -> 1.0)  // 2. Strings will be null
    .build();

Product product = fixtureMonkey.giveMeBuilder(Product.class)
    .set("name", "Override")  // 3. This specific value overrides the null setting
    .sample();

// Result: product.name will be "Override" despite the String null setting
```

---

Understanding these concepts will help you use Fixture Monkey's options more effectively to create the exact test data you need. In the next sections, we'll explore specific options you can use to customize your test objects.
